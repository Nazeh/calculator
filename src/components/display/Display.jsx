import React from 'react';
import PropTypes from 'prop-types';
import './Display.scss';

const Display = () => {
  const stuff = 'grara';

  return (
    <div className="Display">
      gree
      {stuff}
    </div>
  );
};

Display.propTypes = {
};

export default Display;
