import React from 'react';
import './Display.scss';

const Display = () => {
  const stuff = 'grara';

  return (
    <div className="App">
      gree
      {stuff}
      {name}
    </div>
  );
};


Display.propTypes = {
};

export default App;
