import React from 'react';
import PropTypes from 'prop-types';
import './Button.scss';

const Button = () => {
  const stuff = 'grara';

  return (
    <div className="App">
      gree
      {stuff}
    </div>
  );
};


Button.propTypes = {
};

export default Button;
